# Array

# Using zeros() --> The array of zeros

# Syntax:
# zeros(shape, dtype=float, order='C', *, like=None)

from numpy import *

num = int(input("Enter the number: "))

arr = zeros(num)

print(arr)
